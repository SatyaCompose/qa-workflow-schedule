# Skills

Skills for this project live in `.claude/commands/` (Claude Code's standard slash-command location). They surface as `/sync`, `/check-credits`, etc. in the chat input.

See [`../commands/`](../commands/) for the actual definitions.

This `skills/` directory is intentionally kept as a placeholder for future user-invocable skill packs.
